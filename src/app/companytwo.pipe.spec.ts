import { CompanytwoPipe } from './companytwo.pipe';

describe('CompanytwoPipe', () => {
  it('create an instance', () => {
    const pipe = new CompanytwoPipe();
    expect(pipe).toBeTruthy();
  });
});
